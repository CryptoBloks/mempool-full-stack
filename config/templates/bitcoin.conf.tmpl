# Bitcoin Core Configuration — Generated by mempool configurator
# Network: {{NETWORK_NAME}}
# DO NOT EDIT — regenerate with: ./scripts/setup/generate-config.sh

{{NETWORK_FLAG}}

server=1
txindex={{TXINDEX}}
prune={{PRUNE}}

[{{NETWORK_SECTION}}]
rpcport={{RPC_PORT}}
rpcbind={{RPC_BIND}}
rpcallowip={{RPC_ALLOWIP}}

# Internal services (electrs, mempool) — full access
{{RPC_AUTH_INTERNAL}}

# RPC Gateway — restricted access (defense-in-depth)
{{RPC_AUTH_GATEWAY}}
{{RPC_WHITELIST_GATEWAY}}
{{RPC_WHITELIST_DEFAULT}}

# Performance
dbcache={{DBCACHE}}
maxmempool={{MAXMEMPOOL}}
maxconnections={{MAXCONNECTIONS}}
rpcworkqueue={{RPCWORKQUEUE}}
rpcthreads={{RPCTHREADS}}
