# Electrs Configuration — Generated by mempool configurator
# Network: {{NETWORK_NAME}}

network = "{{ELECTRS_NETWORK}}"
daemon_rpc_addr = "bitcoind-{{NETWORK}}:{{RPC_PORT}}"
daemon_p2p_addr = "bitcoind-{{NETWORK}}:{{P2P_PORT}}"
electrum_rpc_addr = "0.0.0.0:50001"
db_dir = "/data"
log_filters = "INFO"
auth = "{{RPC_USER}}:{{RPC_PASS}}"
